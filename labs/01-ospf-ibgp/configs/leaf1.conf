## leaf1 — lab 01 full config (OSPF + iBGP-EVPN + L2VNI)
##
## STUB. This file is consumed by scripts/switch.sh via `load override
## terminal`, so it must be Junos HIERARCHICAL (curly-brace) format — NOT set
## format. It will be generated once the steps/ configs are validated on a live
## vJunos fabric (stacking steps 1→5 == this file).
##
## Until then, follow labs/01-ospf-ibgp/steps/ by hand after deploy.sh.
